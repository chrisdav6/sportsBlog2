<%- include partials/header.ejs %>

<section id="categories" class="py-5">
  <div class="container">
    <h1>
      <%= title %>
    </h1>
    <hr class="mb-5">

    <div class="row">
      <div class="col">
        <ul class="list-group">
          <% categories.forEach((category) => { %>
          <li class="list-group-item">
            <a href="/articles/category/<%= category._id %>" class="text-dark"><%= category.title %></a></li>
          <% }); %>
        </ul>
      </div>
    </div>
  </div>
</section>

<%- include partials/footer.ejs %>